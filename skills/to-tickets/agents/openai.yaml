interface:
  display_name: "To Tickets"
  short_description: "Split an approved spec into executable ticket slices"
  default_prompt: "Use $to-tickets to turn this approved specification into dependency-aware tickets."
policy:
  allow_implicit_invocation: false
